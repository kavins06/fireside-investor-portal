---
description: Show all current deals on the Fireside investor portal
---

Show me all current deals on the **Fireside Investor Portal**. Run the bundled publisher
CLI — `node "${CLAUDE_PLUGIN_ROOT}/scripts/fireside.mjs" list` to get the slugs, then
`get <slug>` for each — and display the results in a clean table: deal name, status,
target IRR, and location. Read-only, no edits.
